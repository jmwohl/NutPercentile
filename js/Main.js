$(function() {
	
	// Our overall **AppView** is the top-level piece of UI.
	//window.peopleList = new PersonCollection;

	// Finally, we kick things off by creating the **App**.
	//window.app = new AppView;
	App.init();
});
